<!DOCTYPE html>
<html>

<head>
    {% include 'header' %}
</head>

<body>
    <div class="container">
        {% include 'sitenav' %}
        
        <h1>Posts</h1>

        <ul>
            {% for post in posts %}
            <li>
                <a href="/post/{{ post.slug }}">{{ post.title }}</a>
            </li>
            {% endfor %}
        </ul>
    </div>
</body>

</html>